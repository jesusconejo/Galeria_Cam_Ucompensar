import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.galeria',
  appName: 'galeria',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
